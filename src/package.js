/**
 * @typedef {import("video.js").default} videojs
 * @typedef {import("crypto-js")} cryptojs
 * @typedef {import("streamsaver")} streamsaver
 */
/** @type {videojs} */
export const videojs = window.videojs;

/** @type {cryptojs} */
export const CryptoJS = window.CryptoJS;

/** @type {streamsaver} */
export const streamsaver = window.streamSaver;
