/** 
 * @typedef {{resolution:string; url:string}[]} VideoUrls
 * @typedef {Record<string,{id:string; isDown:boolean; name:string; input:undefined|HTMLInputElement}>} VideoList
 * @typedef {Record<string,VideoList[0] & {name:string; ids:string[]}>} StoryList
 */;

export { };